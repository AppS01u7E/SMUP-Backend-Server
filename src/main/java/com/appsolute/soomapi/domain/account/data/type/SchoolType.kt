package com.appsolute.soomapi.domain.account.data.type

@kotlin.Deprecated(message = "global.school.SchoolType 을 사용해주세요(Else 보단 나중에 학교추가되면 Type 추가하는식으로하는게 더 좋아보입니다)", level = DeprecationLevel.ERROR)
enum class SchoolType {
    DAEDEOK,
    GWANGJU,
    BUSAN,
    DAEGU,
    ELSE
}